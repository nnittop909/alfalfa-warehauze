<%= simple_form_for(@operator) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
  	<%= f.association :equipment %>
    <%= f.input :first_name %>
    <%= f.input :last_name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
